This is a fun and simple side project

This project does not have to think about all usecases.

Do not test everything. Let's rely mostly on types and only add tests if there is some important business logic.

Always foucs on readable code. That means as little code as possible. Well structured files. Well named variables.

I always lean towards easy and simple instead of perfect and over-engineered
